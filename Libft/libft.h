//incluir header 42

#ifndef LIBFT_H
#define LIBFT_H

#include <string.h>
#include <unistd.h>
#include <stdlib.h>

void	*ft_memset(void *dest, int val, size_t len);
void	*ft_bzero(void *to, size_t count);

#endif
